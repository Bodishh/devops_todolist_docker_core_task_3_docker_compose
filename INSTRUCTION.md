So, to run containers you need this command:
docker-compose up -d

To stop containers you need to use:
docker-compose stop

to clear all docker images,volumes and containers you can use command:
docker system prune -a